services:
  # Frontend - Next.js Web Application
  mediax-web:
    build:
      context: ./frontend
      dockerfile: Dockerfile
      target: development
    container_name: mediax-web
    ports:
      - '3004:3000'
    volumes:
      - ./frontend:/app
      - /app/node_modules
      - /app/.next
    environment:
      - NEXT_PUBLIC_API_URL=http://localhost:4000/api/v1
      - NEXT_PUBLIC_WS_URL=http://localhost:4000
      - NEXT_PUBLIC_APP_NAME=GEN21 MediaX AI
      - NEXT_PUBLIC_ENABLE_AI_FEATURES=true
    depends_on:
      - mediax-api
    networks:
      - adcp-network
    restart: unless-stopped

  # Backend - NestJS API
  mediax-api:
    build:
      context: ./backend
      dockerfile: Dockerfile
      target: development
    container_name: mediax-api
    ports:
      - '4000:4000'
    volumes:
      - ./backend:/app
      - /app/node_modules
    environment:
      # Application
      - NODE_ENV=development
      - PORT=4000
      - API_PREFIX=api/v1

      # Database
      - DATABASE_HOST=mediax-postgres
      - DATABASE_PORT=5432
      - DATABASE_USERNAME=mediax
      - DATABASE_PASSWORD=mediax_password
      - DATABASE_NAME=mediax
      - DATABASE_SYNCHRONIZE=true
      - DATABASE_LOGGING=true
      - DATABASE_SSL=false

      # JWT
      - JWT_SECRET=mediax-super-secret-jwt-key-change-in-production
      - JWT_EXPIRATION=1d
      - JWT_REFRESH_SECRET=mediax-refresh-secret-key-change-in-production
      - JWT_REFRESH_EXPIRATION=7d

      # Redis
      - REDIS_HOST=mediax-redis
      - REDIS_PORT=6379

      # MinIO
      - MINIO_ENDPOINT=minio  # Use service name, not http://
      - MINIO_PORT=9000
      - MINIO_ACCESS_KEY=minioadmin
      - MINIO_SECRET_KEY=minioadmin
      - MINIO_BUCKET_ORIGINALS=content-originals
      - MINIO_BUCKET_PROCESSED=content-processed
      - MINIO_BUCKET_ARTIFACTS=extraction-artifacts
      - MINIO_USE_SSL=false

      # CDN
      - CDN_BASE_URL=http://localhost:9000

      # Kafka
      - KAFKA_ENABLED=true
      - KAFKA_BROKERS=kafka:9092  # Changed from adcp-kafka to kafka
      - KAFKA_CLIENT_ID=mediax-ai
      - KAFKA_GROUP_ID=mediax-consumers

      # Metadata Extraction Layer
      - METADATA_API_URL=http://metadata-layer:3005

      # CORS
      - CORS_ORIGIN=http://localhost:3000,http://localhost:3004
      - CORS_CREDENTIALS=true
    depends_on:
      - mediax-postgres
      - mediax-redis
      - minio
      - kafka  # Added Kafka dependency
    healthcheck:
      test: ['CMD', 'wget', '--no-verbose', '--tries=1', '--spider', 'http://localhost:4000/api/v1/health']
      interval: 10s
      timeout: 5s
      retries: 5
      start_period: 40s
    networks:
      - adcp-network
    restart: unless-stopped

  # PostgreSQL Database for MAM with pgvector for AI embeddings
  mediax-postgres:
    image: pgvector/pgvector:pg16
    container_name: mediax-postgres
    ports:
      - '5433:5432'
    environment:
      - POSTGRES_DB=mediax
      - POSTGRES_USER=mediax
      - POSTGRES_PASSWORD=mediax_password
    volumes:
      - mediax_postgres_data:/var/lib/postgresql/data
      - ./backend/init-pgvector.sql:/docker-entrypoint-initdb.d/init-pgvector.sql
    networks:
      - adcp-network
    healthcheck:
      test: ['CMD-SHELL', 'pg_isready -U mediax']
      interval: 10s
      timeout: 5s
      retries: 5
    restart: unless-stopped

  # Redis for MAM (separate from metadata extraction)
  mediax-redis:
    image: redis:7-alpine
    container_name: mediax-redis
    ports:
      - '6381:6379'
    volumes:
      - mediax_redis_data:/data
    networks:
      - adcp-network
    healthcheck:
      test: ['CMD', 'redis-cli', 'ping']
      interval: 10s
      timeout: 5s
      retries: 5
    restart: unless-stopped

  # MinIO (shared with metadata extraction)
  minio:
    image: minio/minio:RELEASE.2024-12-13T22-19-12Z
    container_name: mam-minio
    ports:
      - '9000:9000'
      - '9001:9001'
    environment:
      - MINIO_ROOT_USER=minioadmin
      - MINIO_ROOT_PASSWORD=minioadmin
      # - MINIO_NOTIFY_KAFKA_ENABLE_PRIMARY=on
      # - MINIO_NOTIFY_KAFKA_BROKERS_PRIMARY=kafka:9092  # Changed from adcp-kafka to kafka
      # - MINIO_NOTIFY_KAFKA_TOPIC_PRIMARY=minio.events
    volumes:
      - minio_data:/data
    command: server /data --console-address ":9001"
    networks:
      - adcp-network
    healthcheck:
      test: ['CMD', 'curl', '-f', 'http://localhost:9000/minio/health/live']
      interval: 30s
      timeout: 10s
      retries: 3
    restart: unless-stopped

  # MinIO Initialization - Create buckets
  minio-init:
    image: minio/mc:latest
    container_name: minio-init
    depends_on:
      - minio
    entrypoint: >
      /bin/sh -c "
      echo 'Waiting for MinIO...';
      until mc alias set adcp http://minio:9000 minioadmin minioadmin; do
        echo 'Waiting for MinIO...';
        sleep 2;
      done;

      echo 'Configuring MinIO client...';
      mc alias set adcp http://minio:9000 minioadmin minioadmin;

      echo 'Creating buckets...';
      mc mb adcp/content-originals --ignore-existing;
      mc mb adcp/content-processed --ignore-existing;
      mc mb adcp/extraction-artifacts --ignore-existing;
      mc mb adcp/strapi-uploads --ignore-existing;

      echo 'Setting bucket versioning...';
      mc version enable adcp/content-originals;

      echo 'Configuring event notifications...';
      # Create separate event rules to avoid overlap errors
      mc event add adcp/content-originals arn:minio:sqs::PRIMARY:kafka \
        --event put \
        --suffix .mp4,.mov,.avi,.mkv;
      mc event add adcp/content-originals arn:minio:sqs::PRIMARY:kafka \
        --event put \
        --suffix .jpg,.jpeg,.png,.webp;
      mc event add adcp/content-originals arn:minio:sqs::PRIMARY:kafka \
        --event delete;

      echo 'MinIO initialization complete!';
      "
    networks:
      - adcp-network

  # Kafka Service - Added missing service
  kafka:
    image: confluentinc/cp-kafka:7.5.0
    container_name: adcp-kafka
    ports:
      - "9092:9092"
    environment:
      KAFKA_BROKER_ID: 1
      KAFKA_ZOOKEEPER_CONNECT: zookeeper:2181
      KAFKA_ADVERTISED_LISTENERS: PLAINTEXT://kafka:9092
      KAFKA_LISTENERS: PLAINTEXT://0.0.0.0:9092
      KAFKA_OFFSETS_TOPIC_REPLICATION_FACTOR: 1
      KAFKA_TRANSACTION_STATE_LOG_MIN_ISR: 1
      KAFKA_TRANSACTION_STATE_LOG_REPLICATION_FACTOR: 1
      KAFKA_AUTO_CREATE_TOPICS_ENABLE: "true"
    depends_on:
      - zookeeper
    networks:
      - adcp-network
    restart: unless-stopped

  # Zookeeper Service (required by Kafka)
  zookeeper:
    image: confluentinc/cp-zookeeper:7.5.0
    container_name: adcp-zookeeper
    ports:
      - "2181:2181"
    environment:
      ZOOKEEPER_CLIENT_PORT: 2181
      ZOOKEEPER_TICK_TIME: 2000
    networks:
      - adcp-network
    restart: unless-stopped

  # Optional: Kafka UI for monitoring
  kafka-ui:
    image: provectuslabs/kafka-ui:latest
    container_name: kafka-ui
    ports:
      - "8080:8080"
    environment:
      KAFKA_CLUSTERS_0_NAME: local
      KAFKA_CLUSTERS_0_BOOTSTRAPSERVERS: kafka:9092
      KAFKA_CLUSTERS_0_ZOOKEEPER: zookeeper:2181
    depends_on:
      - kafka
    networks:
      - adcp-network
    restart: unless-stopped

networks:
  adcp-network:
    name: adcp-network
    driver: bridge

volumes:
  mediax_postgres_data:
    driver: local
  mediax_redis_data:
    driver: local
  minio_data:
    driver: local
  kafka_data:
    driver: local
  zookeeper_data:
    driver: local